#include "stdio.h"
/**
 * main - entry point
 * @argv: array of entered arguments
 * @argc: count for entered arguments
 * Return: none
*/
int main(int argc, char const **argv)
{
(void) argc;
printf("%s\n", argv[0]);
return (0);
}
