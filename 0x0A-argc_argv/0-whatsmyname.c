#include <stdio.h>
/**
 * main - entry point
 * @argc: not used parameter but used for parameters counting
 * @argv: array of poointers to the input strings
 * Return: zero in sucess
*/
int main(int argc, char **argv)
{
(void)argc;
printf("%s\n", argv[0]);
return (0);
}
