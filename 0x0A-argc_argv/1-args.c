#include <stdio.h>
/**
 * main - entry point for a function prints
 * the passed arguments number
 * @argc: argument count
 * @argv: array of pointers to passed strings
 * Return: zero in sucess
*/
int main (int argc, char **argv)
{
(void)argv;
printf("%d\n", argc);
return (0);
}
