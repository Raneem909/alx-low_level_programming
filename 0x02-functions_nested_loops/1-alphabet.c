#include "main.h"
/**
 * main - entry point
 * Description: prints a-z
 * Return: always 0
*/
int main(void)
{
int count;
for (count = 'a'; count <= 'z'; count++)
_putchar(count);
_putchar('\n');
return (0);
}
